# 2026-07-08 工作记忆

## AI 自治多智能体项目（Magic Arena 魔法竞技场）
- 位置：根目录 5 控制文件 + `magic-arena/` 子目录（game.js / index.html / dist/ / DESIGN.md / test/）。
- 运行模型：**方向驱动（Direction-Driven, D20）**——不设终点，每次调用须在 5 个方向（核心玩法/内容扩建/系统新创/体验打磨/工程基石）至少推进一个；仅用户 `freeze` 停。
- 约束（自动化任务硬性）：每次 ≤5 个业务文件改动、禁网络、日志写根目录 `log.md`（小时精度，不计入 5 文件上限）。
- 测试套件（纯 Node 零依赖）：`node test/smoke-test.js` / `test/status-effects.test.js` / `test/perf-check.js` / `test/balance-scan.js`。

## 本日 10:00 轮（第 41 轮自治 · 方向5 工程基石）
- 为最近两个特性补确定性回归测试：@A38 施法目标预览（S8：computeValidTargets 契约）+ @A37 单位图鉴（S9：renderCodex 契约）。
- smoke 22/0 → **33/0**（+11 断言）；status-effects 16/0；balance-scan 退出码0（梯度6/6/0 单调）；perf 8/0；全绿无回归。
- 改动文件：magic-arena/test/smoke-test.js、BLACKBOARD.md（LOG.md 免费）。
